{
  "name": "mephi-server",
  "version": "1.0.0",
  "description": "Simple HTTP server for MEPHI",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/server.test.js",
    "sbom": "cyclonedx-bom -o sbom.json",
    "scan": "dependency-check --scan . --format HTML --format JSON --out reports/"
  },
  "devDependencies": {
    "@cyclonedx/cyclonedx-npm": "^2.0.0",
    "dependency-check": "^5.0.0"
  }
}